// PLACEHOLDER. This file is overwritten by `flutterfire configure`.
// Until that's run, [DefaultFirebaseOptions.currentPlatform] throws and the
// app gracefully falls back to mocked auth/firestore. See .scratch/HUMAN_TODO.md
// step 5 for the one-line command that replaces this stub.

import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'firebase_options.dart has not been generated yet. '
      "Run 'flutterfire configure --platforms=web' from the repo root. "
      'The app will run on mocked auth/firestore until then.',
    );
  }
}
